# Sentry DSN for the frontend project (create at sentry.io, platform "React").
# Vite embeds this into the built bundle at build time. Unset = Sentry.init
# never runs, no error reporting, everything else works unaffected.
VITE_SENTRY_DSN=
